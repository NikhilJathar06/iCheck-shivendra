import { CommaSeperatedDirective } from './comma-seperated.directive';

describe('CommaSeperatedDirective', () => {
  it('should create an instance', () => {
    const directive = new CommaSeperatedDirective();
    expect(directive).toBeTruthy();
  });
});
